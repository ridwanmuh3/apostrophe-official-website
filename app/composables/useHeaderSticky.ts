// export default function () {
//   const isSticky = useState<boolean>("isSticky", () => false);
//   const isStickyAnimating = useState<boolean>("isStickyAnimating", () => false);
//   let animationTimeout: ReturnType<typeof setTimeout> | null = null;

//   return {} || null;
// }
